// Import asciinema-player
import * as AsciinemaPlayer from "asciinema-player";

// Make AsciinemaPlayer available globally
window.AsciinemaPlayer = AsciinemaPlayer;

// Initialize players when DOM is ready
document.addEventListener("DOMContentLoaded", function () {
  // Players are initialized inline in the HTML template
  // This just ensures the library is loaded and available
});
